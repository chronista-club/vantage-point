# Rust
/target/

# Xcode
.build/
.swiftpm/
*.xcodeproj/
*.xcuserdata/
*.xcworkspace/xcuserdata/
DerivedData/
*.xcscmblueprint
*.xccheckout

# macOS
.DS_Store
*.app
*.app.zip

# 移行中のファイル（ルートディレクトリのみ）
/ClaudeTestApp/

# Working directory (for temporary files and experiments)
/working/

# Git worktrees
/.worktrees/

# Personal notes (not tracked in git)
/.notes/

# Serena memories (per-user, not tracked)
.serena/
.env

# Local settings (per-user, not tracked)
*.local.*
.claude/worktrees/
.claude/scheduled_tasks.lock
.gitnexus

# xcodebuild 中間成果物
/apple/VantagePoint/build/

# Release 配布物（DMG 等）
/dist/


# Per-user MCP overlay (add extra servers without leaking credentials)
.mcp.local.json

# vp-app dev log (mise win task の stderr redirect 先)
vp-*.log
