# webview bundle は生成物 (mise run app:bundle / bun run build)。2026-07-19 に commit 運用を廃止 —
# 依存の npm 化 (semver pin + bun.lock) で「commit で内容を pin する」必要が消えた。
*.bundle.js
# xterm.css は build.mjs が node_modules/@xterm/xterm から複写する (2026-07-26)。
# xterm 本体 + addon の bundle 取り込みと対。手で vendored copy を持つと bun update で版ズレする。
xterm.css
