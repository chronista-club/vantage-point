{
  "name": "vp-app-sidebar-bundle",
  "version": "0.0.1",
  "description": "vp-app sidebar 用 minimal JS bundle (iconify-icon Web Component register、 SolidJS なし)",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun run build.mjs",
    "build:dev": "bun run build.mjs --dev"
  },
  "dependencies": {
    "iconify-icon": "^3.0.2"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "typescript": "^5.9.0"
  }
}
