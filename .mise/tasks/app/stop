#!/usr/bin/env ruby
# frozen_string_literal: true
#MISE description="vp-app GUI を停止 (`vp app stop` の thin wrapper、 daemon / tmux / SP は触らない)"
#
# file-based mise task 移行 (2026-05-30)。 旧 [tasks."app:stop"] (exec vp app stop) を移植。
require File.join(ENV.fetch("MISE_PROJECT_ROOT"), "scripts/mise/vp.rb")

VP.exec(VP.cargo_bin("vp"), "app", "stop")
