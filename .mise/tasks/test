#!/usr/bin/env ruby
# frozen_string_literal: true
#MISE description="Run tests"
#
# file-based mise task 移行 (2026-05-30)。 旧 [tasks.test] (run = "cargo test") を Ruby file task に移植。
#
# mbx 経由 (2026-08-30 採用)。理由と実測は `.mise/tasks/check` の doc を参照
# (初回は 2 倍遅い / 2 回目以降 59 倍 / release 系は cargo のまま)。
require File.join(ENV.fetch("MISE_PROJECT_ROOT"), "scripts/mise/vp.rb")

VP.sh "mbx test"
